server {
    listen 8080;
    
    location / {
    	root www/html;
        index index.html;
        autoindex on;
        allowed_methods GET;
    }

    location /uploads {
    	root www/html;
        upload_dir /tmp/uploads;
        allowed_methods POST;
    }
}

