events {}

http {
    server {
        listen 8080;
        server_name localhost;

        location / {
            # This will show you the request method and body size
            return 200 "Request method: $request_method\nRequest body size: $request_length\n";
        }
    }
}
